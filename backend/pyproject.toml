[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "scripts"]
asyncio_default_fixture_loop_scope = "function"
markers = ["slow: exercises run.sh as a real subprocess (spawns Python + trains/predicts)"]
